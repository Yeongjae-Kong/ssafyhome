<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.home.model.dao.RefreshTokenDao">

    <resultMap id="RefreshTokenMap" type="com.ssafy.home.model.dto.RefreshToken">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="token" property="token"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.ssafy.home.model.dto.RefreshToken" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens(user_id, token)
        VALUES(#{userId}, #{token})
    </insert>

    <select id="selectByToken" parameterType="string" resultMap="RefreshTokenMap">
        SELECT id, user_id, token, created_at FROM refresh_tokens WHERE token = #{token}
    </select>

    <delete id="deleteByToken" parameterType="string">
        DELETE FROM refresh_tokens WHERE token = #{token}
    </delete>

    <delete id="deleteByUserId" parameterType="int">
        DELETE FROM refresh_tokens WHERE user_id = #{userId}
    </delete>

</mapper>

